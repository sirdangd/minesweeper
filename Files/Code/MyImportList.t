% import class list
% this is the very first file to be included in includes.t
% Place class files into the Classes folder and then
% add add the classes to be import statement by using the 
% list operator "," , the class name followed by its relative path.
% example to a class named MyClass.cla located in classes folder
%      import GUI, MyClass in "../../Classes/MyClass.cla"
%                      ../ means go up a folder
import GUI
